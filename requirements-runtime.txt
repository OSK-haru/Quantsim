# Direct dependencies required by the Python simulator and FastAPI.
# Use requirements-lock.txt instead to reproduce the complete audited environment.
fastapi==0.138.1
numpy==2.4.4
pydantic==2.13.4
uvicorn==0.49.0
